@model List<MVC_StokTakip.Models.Entity.Urunler>



    <li class="nav-item dropdown">
        <a class="nav-link" data-toggle="dropdown" href="#">
            <i class="far fa-bell"></i>
            <span class="badge badge-warning navbar-badge">@Model.Count</span>
        </a>
      
        <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
            <span class="dropdown-item dropdown-header">@Model.Count Bildirim</span>

            @foreach (var item in Model)
            {
                <div class="dropdown-divider"></div>
                <a href="/Urunler/GuncelleBilgiGetir/@item.ID" class="dropdown-item">
                    <i class="fas fa-envelope mr-2"></i>  @item.BarkodNo - @item.Miktari adet kalmıştır.
                  
                </a>

                }
                <div class="dropdown-divider"></div>
                <a href="#" class="dropdown-item dropdown-footer">See All Notifications</a>
            </div>
      
        </li>