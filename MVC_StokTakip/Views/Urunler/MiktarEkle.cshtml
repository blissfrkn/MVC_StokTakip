@model MVC_StokTakip.MyModel.MyUrunler
@{
    ViewBag.Title = "MiktarEkle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


    <div class="container">
        <h2>Ürün Miktar Ekleme</h2>
        @using (Html.BeginForm("MiktarEkle", "Urunler", FormMethod.Post))
        {
            @Html.HiddenFor(x => x.ID)
            @*<div class="form-group">
                @Html.LabelFor(x => x.ID)
                @Html.TextBoxFor(x => x.ID, new { @class = "form-control" })
            </div>*@
            <div class="form-group">
                @Html.LabelFor(x => x.Miktari)
                @Html.TextBoxFor(x => x.Miktari, new { @class = "form-control", @placeholder = "Eklenecek adeti giriniz." })
            </div>
            <div><button class="btn btn-success">Miktar Ekle</button></div>
        }

    </div>

